require "spec_helper"

describe Model2Factory::Rails do
  it "has a version number" do
    expect(Model2Factory::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
